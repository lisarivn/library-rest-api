version: '3.8'

services:
  app:
    image: maven:3.9.6-eclipse-temurin-17
    working_dir: /app
    command: mvn spring-boot:run
    volumes:
      - ./:/app
      - ~/.m2:/root/.m2 
    environment:
      - SPRING_DEVTOOLS_RESTART_ENABLED=true
    ports:
      - "8081:8081"
      - "5005:5005"